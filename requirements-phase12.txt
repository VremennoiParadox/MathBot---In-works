# MathBot Phase 1 + 2 only (terminal: capture, vision, solve, recheck)
# Does NOT install pyautogui / opencv — avoids pyobjc compile errors until Phase 3.
# Requires Python 3.11+ (brew install python@3.12)

mss>=9.0.0
pillow>=10.0.0
pynput>=1.7.6
requests>=2.31.0
ollama>=0.3.0
psutil>=5.9.0
imagehash>=4.3.1

# Optional for developers
pytest>=7.4.0
