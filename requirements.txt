# Full runtime (Phase 3+ automation needs pyautogui → pyobjc on macOS).
# For Phase 1–2 only, use: pip install -r requirements-phase12.txt
# Requires Python 3.11+ — do not use old Xcode Python 3.9.

# --- runtime ---
mss>=9.0.0
pyautogui>=0.9.54
pillow>=10.0.0
imagehash>=4.3.1
pynput>=1.7.6
requests>=2.31.0
ollama>=0.3.0
opencv-python>=4.8.0
psutil>=5.9.0
pytest>=7.4.0

# --- [build] — not installed for end users; dev/release only ---
# pip install pyinstaller>=6.3.0
pyinstaller>=6.3.0
