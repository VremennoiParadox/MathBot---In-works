# Graph test fixtures

Place sample graph screenshots here for manual testing:

```bash
python graph_solver.py   # one-shot graph solve (needs config + Ollama)
```

Optional: `bar_chart.png`, `line_graph.png` for visual QA.
